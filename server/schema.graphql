type User {
  id: ID!
  email: String!
}

type AuthPayload {
  user: User!
  token: String!
}

type Query {
  currentUser: User!
}

type Mutation {
  login(email: String!, password: String!): AuthPayload!
}
