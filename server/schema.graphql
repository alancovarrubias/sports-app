type User {
  id: ID!
  email: String!
}

type AuthPayload {
  token: String!
  user: User!
}

type Query {
  currentUser: User!
}

type Mutation {
  login(email: String!, password: String!): AuthPayload!
}
